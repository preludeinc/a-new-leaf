<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Final" parent="Theme.Material3.DayNight.NoActionBar">
    </style>

    <style name="Theme.Final" parent="Base.Theme.Final" />

    <style name="AlertDialogCustom" parent="@android:style/Theme.Dialog">
        <item name="android:windowNoTitle">true</item>
        <item name="android:background">@color/background_blue</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:fontFamily">@font/sugary_pancake</item>
        <item name="android:textSize">16sp</item>
    </style>

</resources>